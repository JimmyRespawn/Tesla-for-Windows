<ContentDialog
    x:Class="TeslaMurphy.Controls.ClimateContentDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:TeslaMurphy.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="Climate"
    Closing="ContentDialog_Closing"
    Loaded="ContentDialog_Loaded"
    SecondaryButtonText="CLOSE"
    Style="{StaticResource DefaultContentDialogStyle}"
    mc:Ignorable="d">
    <StackPanel>
        <Grid>
            <Image
                Grid.Row="1"
                MaxWidth="240"
                Margin="24,12"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Top"
                Source="/Assets/Images/TelsaBirdViewPhoto.webp" />
        </Grid>
        <StackPanel HorizontalAlignment="Center" Orientation="Horizontal">
            <TextBlock
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                Style="{StaticResource CaptionTextBlockStyle}"
                Text="Interior" />
            <TextBlock
                x:Name="InteriorTemperatureTextBlock"
                Margin="4,0,0,0"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                Style="{StaticResource CaptionTextBlockStyle}" />
            <TextBlock
                Margin="4,0"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                Style="{StaticResource CaptionTextBlockStyle}"
                Text="·" />
            <TextBlock
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                Style="{StaticResource CaptionTextBlockStyle}"
                Text="Exterior" />
            <TextBlock
                x:Name="ExteriorTemperatureTextBlock"
                Margin="4,0,0,0"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                Style="{StaticResource CaptionTextBlockStyle}" />
        </StackPanel>
        <Grid Margin="0,24,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <TextBlock VerticalAlignment="Center" Text="Climate control" />
            <TextBlock
                Grid.Row="1"
                Margin="0,4,20,0"
                VerticalAlignment="Center"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                MaxLines="2"
                Style="{StaticResource CaptionTextBlockStyle}"
                Text="Heat or cool the cabin"
                TextWrapping="Wrap" />
            <ToggleSwitch
                x:Name="ACModeSwitch"
                Grid.RowSpan="2"
                Grid.Column="1"
                MinWidth="0"
                VerticalAlignment="Center"
                IsEnabled="False"
                OffContent=""
                OnContent=""
                Toggled="ACModeSwitch_Toggled" />
        </Grid>
        <Grid Margin="0,24,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <TextBlock VerticalAlignment="Center" Text="Auto mode" />
            <TextBlock
                Grid.Row="1"
                Margin="0,4,20,0"
                VerticalAlignment="Center"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                MaxLines="2"
                Style="{StaticResource CaptionTextBlockStyle}"
                Text="Last reported by the vehicle. Read only."
                TextWrapping="Wrap" />
            <ToggleSwitch
                x:Name="AutoACModeSwitch"
                Grid.RowSpan="2"
                Grid.Column="1"
                MinWidth="0"
                VerticalAlignment="Center"
                IsEnabled="False"
                OffContent=""
                OnContent="" />
        </Grid>
        <ProgressRing
            x:Name="CommandProgress"
            Width="24"
            Height="24"
            IsActive="False" />
        <TextBlock
            x:Name="StatusTextBlock"
            Margin="0,12,0,0"
            TextWrapping="Wrap" />
    </StackPanel>
</ContentDialog>
